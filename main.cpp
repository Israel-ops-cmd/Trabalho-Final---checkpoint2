#include "ControladorDeTransito.h" // Inclui a definição da classe ControladorDeTransito, que gerencia a lógica do sistema.

int main() {
    ControladorDeTransito controlador; // Cria uma instância da classe ControladorDeTransito para gerenciar a interação com o usuário.
    controlador.iniciar(); // Chama o método 'iniciar' do controlador, que começa a interação e a execução das funcionalidades do sistema.

    return 0; // Retorna 0 para indicar que o programa terminou com sucesso.
}
